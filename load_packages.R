#################
# load R package
#################

install.packages(c("psych", "ftsa", "meboot", "pracma", "reldist", "flexmix", "demography"))
require(psych)
require(ftsa)
require(meboot)
require(pracma)
require(reldist)
require(flexmix)
require(demography)
